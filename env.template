# ============================================
# CLERK AUTHENTICATION
# ============================================
# Get these from: https://dashboard.clerk.com/
# 1. Create a new application
# 2. Enable Google OAuth in "User & Authentication" > "Social Connections"
# 3. Copy keys from "API Keys" section

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
CLERK_SECRET_KEY=sk_test_your_secret_key_here

# Clerk Webhook Secret
# Get from: Clerk Dashboard > Webhooks > Create Endpoint
# Endpoint URL: https://yourdomain.com/api/webhooks/clerk
# Subscribe to events: user.created, user.updated, user.deleted
CLERK_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Clerk Redirect URLs (leave as is for local development)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# ============================================
# SUPABASE DATABASE
# ============================================
# Get these from: https://supabase.com/dashboard
# 1. Create a new project
# 2. Wait for database to be ready
# 3. Go to "Settings" > "API"

# Project URL (found in Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Anon/Public Key (found in Settings > API > Project API keys > anon public)
# Safe to use in browser - respects Row Level Security
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Service Role Key (found in Settings > API > Project API keys > service_role)
# ⚠️ NEVER expose this to the client! Server-side only!
# This key bypasses Row Level Security
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================
# NOTES
# ============================================
# - Copy this file to .env.local and fill in your actual keys
# - After setting up Clerk, configure the webhook endpoint to sync users to Supabase
# - Run the Supabase migration at supabase/migrations/001_initial_schema.sql
# - To promote a user to admin: UPDATE profiles SET role = 'admin' WHERE clerk_id = 'user_xxx';
# - To promote a user to superadmin: UPDATE profiles SET role = 'superadmin' WHERE clerk_id = 'user_xxx';

